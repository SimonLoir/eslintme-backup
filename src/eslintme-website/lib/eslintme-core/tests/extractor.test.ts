import * as fs from 'fs';
import Extractor from '../src/Extractor';

describe('core', () => {
    let e: Extractor;

    beforeAll(() => {
        e = new Extractor();
    });

    test('test', () => {});
});

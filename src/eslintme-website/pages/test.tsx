import RuleUI from '@components/RuleUI';

/**
 * Component representing a page created to perform tests on new features.
 */
export default function TestPage() {
    return (
        <>
            <RuleUI name={'yoda'} />
        </>
    );
}
